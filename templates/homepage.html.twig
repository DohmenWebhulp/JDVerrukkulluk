{# Opnemen van de "Main" file #}
{% extends "main.html.twig" %}

{# nu de block content op in de main pagina vullen #}
{% block content %}
    {# Dit is commentaar #}

    {# Print #}

    <h4>U bevindt zich nu op de {{  title }}</h4>

{# Loop #}
    <div class="row">
    {% for record in data %}
        <div class="receptinfoh">
            <div class="row">
                <div class="col-md-12">
                    <img src="{{record.foto}}" style="width: 100%; height: 100%">
                </div>
                <div class="col-md-12">
                    <div class="flotl" style="padding-top: 20px">
                        <i class="fas fa-user-friends"></i> 4
                        <i class="fas fa-euro-sign" style="margin-left: 5px"></i> {{record.Totale_Prijs}},- 
                        <i class="fab fa-gripfire" style="margin-left: 5px"></i> {{record.Totaal_Calorieën}}
                    </div>
                    <div style="clear: both"></div>
                    <div class="flotl"><h5>Heerlijke {{record.Naam}}!</h5></div>
                    <div class="flotr">
                        <span class="fa fa-star checked"></span>
                        <span class="fa fa-star checked"></span>
                        <span class="fa fa-star checked"></span>
                        <span class="fa fa-star"></span>
                        <span class="fa fa-star"></span>
                    </div>
                    <div style="clear: both"></div>
                    <div style="clear: both">{{record.korteOmschrijving}}</div>

                    <!--JavaScript redirect naar boodschappenlijst-->

                    <a href="index.php?action=detailpage&id={{record.id}}" type="button" class="btn btn-primary butn flotl" style="margin-top: 30px">
                        Smullen!
                    </a>
                    
                    <!--ajax call met JavaScript-->


                </div>
            </div>
        </div>
    {% endfor %}
    </div>

{% endblock %}